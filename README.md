The packages in requirements.txt are needed

This python script will run a streamlit  version and the user can interactively change the ticker symbol and see the score 

The command : streamlit run stock_analyzer_pro_streamlit.py
